# 评测样例目录

M5 阶段补足不少于 50 组评测样例，用于离线复现：

- 幻觉率 `< 5%`
- 难度匹配准确率 `>= 85%`
- 核心知识覆盖率 `>= 90%`
- 学习路径顺序准确率（如实现）

这里的 `p0_cases.json` 是资源生成、审核和画像决策的端到端 MVP 基线，保留其既有
`AIAPP-Kxxx` 标识，不作为知识检索召回金标准。Knowledge Retrieval Agent 的 30 条开发集和
20 条冻结验收集位于 `data/rag_evaluation/`，由独立脚本加载。
